# Nigerian Road Risk Reporter - Streamlit Cloud Compatible
# Core dependencies
streamlit>=1.28.0
pandas>=2.1.0
plotly>=5.17.0

# Security dependencies (Streamlit Cloud compatible)
bcrypt>=4.0.1
pyotp>=2.9.0

# Data processing
numpy>=1.26.0

# Note: Removed problematic dependencies for Streamlit Cloud:
# - python-magic (not available on Streamlit Cloud)
# - redis (not needed for basic functionality)
# - cryptography (bcrypt is sufficient for password hashing) 